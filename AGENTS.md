# Project guidance

Use the TypeSafe skill requested by the user when working on this project. It is installed at `/Users/xialiwei/.agents/skills/typesafe-ai/SKILL.md`; read the skill and relevant live docs before any TypeSafe integration. Keep deterministic settings and explicit user choices in code, as the skill directs. Do not claim TypeSafe API validation when no API request was made.

For iOS changes, preserve iOS 12 compatibility and the existing Electron behavior. Keep account credentials separate from preferences and model selection. Distinguish mocked API tests, physical-device UI tests and real paid synthesis in validation reports.
