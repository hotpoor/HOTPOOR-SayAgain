# 开发传记

按日期正序，从项目起点回顾演进。最新进度见 [开发日志](DEVELOPMENT_LOG.md)。

记录时间使用北京时间（Asia/Shanghai），表示记录整理时间，不代表任务耗时。功能实现状态以 [README](README.md) 为准。

记录原则：如实保留尝试过的策略、用户明确反馈、失败与不满意、调整依据和未验证效果。区分用户反馈、开发者推断、自动测试、真实样例与用户验收；回溯整理注明整理时间，不猜测历史发生时间，不用后来的成功抹去早期问题。

维护方式：在 DEVELOPMENT_LOG.md 添加格式为 `## YYYY-MM-DD HH:mm · 标题` 的记录，然后运行 `node scripts/sync-development-history.mjs`。传记与图表自动生成；运行时加 `--check` 检查同步。请勿单独编辑传记和图表。

### 每日工作记录分布

<img src="docs/charts/development-activity.svg" width="720" height="174" alt="过去 365 天的每日开发记录分布，绿色越深记录越多">

以最新日志日期为终点展示 365 天，每格一天；统计来自开发记录，不是 GitHub 提交数，也不代表工作时长。

<details>
<summary>展开本月明细 · 查看每天的日期和记录数</summary>

<img src="docs/charts/development-month.svg" width="720" alt="最新日志所在月份的每日记录数">

</details>

### 每日记录概览

共 **76 条开发记录**，覆盖 **4 个日期**。一条记录可以包含多项相关工作。

| 日期 | 开发记录 |
| --- | ---: |
| 2026-09-24 | 33 |
| 2026-09-25 | 37 |
| 2026-09-26 | 2 |
| 2026-09-27 | 4 |

## 2026-09-24 07:48 · 项目目标、数据协议与桌面方向

完成首版公开项目说明：以真实对话中的表达为练习材料，选择母语与目标语言，保留优化原因和可复用句型，用自己的音色反复听练。本地优先，云端 API 暂缓。

- 整理语言配置、逐轮评估、多音色与录音样本、备注、创建日期、归档与历史保留规则。
- 确定三个 SQLite 文件：SayAgain 存索引；SayAgain1 / SayAgain2 各只有 entities 表。使用 32 位小写十六进制 UUID v4，按数值求余路由，时间精度为毫秒。
- 编写索引与实体建表 SQL、虚构 JSON 示例，记录事务一致性、缓存和备份方案。
- 确定 Electron 客户端方向：页面功能与展示参考已有 Nexplay 线上版，界面布局与视觉参考 Codex 和 ChatGPT 客户端；保留浅灰标签、宽松条目间距、双栏细竖线和默认展开的音轨。
- 增加按日期逆序的开发日志、正序的开发传记，共用每日贡献格、可展开月历和日期统计；使用 Node.js 脚本从日志生成，提供同步检查。

验证：SQLite 建表与五条虚构实体写入、无效 ID / JSON / 时间戳约束、跨库事务回滚和 UUID 分库计算已做基础检查。文档和图表同步、正逆日期排序、重复生成一致性、365 个日期无重复、跨年与闰年二月、Markdown 链接及示例 JSON 检查通过。

当前限制：尚无 Electron 应用、Skill、存储服务或 TTS worker 实现；未下载模型，未进行本地音色合成与客户端接入测试。线上原型不是本仓库的已实现客户端，也未将其中的个人对话和音频导入公开仓库。

下一步：实现本地三库存储与 Electron 基础窗口，再接语言设置、音色管理和表达回顾。

## 2026-09-24 09:11 · Electron 源码客户端与本地音色库

将项目从数据设计推进到可运行的本地客户端。采用 Electron 44.4.5、原生 HTML/CSS/JavaScript 与 Node 内置 SQLite，基础客户端不依赖 Python。

- 实现首次语言设置、手动表达录入、原句/建议/解释/句型回顾、搜索、收藏、归档恢复与语言对筛选；历史表达保留原语言对。
- 按线上版内容结构与已确认的 Codex / ChatGPT 风格方向实现侧栏、白底黑白灰、浅灰标签、大留白、无外层卡片、宽窗口双栏细竖线、窄窗口单栏及全屏切换。
- 实现 SayAgain / SayAgain1 / SayAgain2 初始化与 UUID 分库、实体和索引跨库事务、去重约束、修订冲突检查、索引重建以及数据库和录音的一致备份。
- 实现多音色与多个参考录音、名称和备注、创建/录制日期、导入音频、麦克风录音、默认音色与样本、归档恢复；归档保留录音并清除默认选择。
- 参考音轨默认展开，展示实际音频波形，支持播放、暂停、拖动进度和 0.75× 慢放；点击不额外增加边框，键盘焦点保持可见。
- 增加受限 preload 业务接口、本地音频协议、上下文隔离、单实例与媒体权限约束；测试使用独立临时数据目录。
- README 增加源码启动方式与虚构示例截图，同步产品、数据和客户端文档。

验证：6 项 SQLite / 业务测试通过，覆盖精确分库结构、UUID 路由、事务回滚、修订冲突、索引重建、历史语言、音色归档及备份重开。Electron 检查覆盖语言引导、表达搜索/收藏、模拟麦克风录音、WAV 导入、多样本默认选择、播放/拖动/慢放、归档恢复、设置、窄窗口、全屏、上下文隔离和退出重开后的数据恢复。宽窄窗口及音色库截图已检查。

当前限制：只验证 macOS；录音使用模拟麦克风，未验证真人录音听感。当前手动录入表达，尚无自动评估、Skill、Qwen3-TTS worker、模型下载器或安装包；备份可在独立目录重开，图形恢复入口未实现。没有调用付费 API 或下载模型权重。

下一步：接通结构化表达评估与 Skill，再接本地 Qwen3-TTS 音色合成和任务队列。

## 2026-09-24 09:55 · Qwen 云端接入、空间预检与 Skill 桥接

因当前设备磁盘空间不足，优先提供千问AI平台云端音色克隆和语音生成入口，继续保留本地优先的产品方向。

- 设置顶部新增 Qwen AK / API Key 密码输入、系统加密保存、移除密钥、启用云端及平台快捷跳转。密钥不回显、不进入数据库或备份；项目 .secret 保持 Git 忽略。
- 接通音色创建、音色复用、合成队列、取消、缓存、音频下载、生成后的波形播放；发送前明确确认参考录音与文字，失败不伪装成功，不自动重试付费请求。
- 新录音与导入音频转为单声道 PCM16 WAV，云端校验时长、大小和采样率。
- 加入本地模型空间预检与固定版本安装器、独立 Qwen3-TTS worker。本机约 8.2 GiB 可用，低于 10 GiB 安装预算，未下载模型或安装其依赖。
- 加入 Skill 协议、命令行客户端、授权 loopback 桥接与可选提示钩子；评估来源、配置版本、Unicode 原句范围与重复提交均校验。未安装全局钩子。
- 修正 Git 忽略规则，避免大小写不敏感文件系统把 skills/sayagain 误判为数据库。

验证：13 项自动测试通过；Electron 检查通过，包括加密 AK 保存、重开恢复、移除及平台跳转。云端使用模拟响应验证，未使用真实 AK 产生付费请求；真实声音克隆听感、本地模型推理及宿主逐轮自动触发尚未验证。

## 2026-09-24 09:59 · 简化空间不足提示样式

移除空间不足提示块的左侧粗边框，避免边框沿圆角弯曲形成突兀的装饰。保留浅灰背景和文字层级，并在左侧加入信息图标；图标与正文顶端对齐，云端入口和本地模型状态使用一致样式。

验证：检查最终 CSS 无该边框规则；仅调整提示样式，不涉及业务逻辑。

## 2026-09-24 10:32 · 母语朗读、实时波形与参考录音裁剪

重做参考录音弹窗，隐藏原生文件输入和原生播放器，以导入按钮、实时麦克风波形和自定义选段试听控件替代。录音停止或导入后，可拖动起止滑块、恢复全段，保存仅保留所选音频，外部原文件不变。

朗读文案跟随母语，先提供默认原创日常叙述，独立放在文案文件中。允许一键填入原文；提醒裁掉朗读内容后同步调整原文。后续计划让 Skill 替换文案，本次未提供运行时替换接口。

验证：Electron 检查覆盖母语文案填入、隐藏原生控件、模拟麦克风录音、波形、选段试听和保存裁剪后的 WAV 时长；音色库与设置回归检查通过。未验证真人麦克风听感。

## 2026-09-24 10:46 · 播放进度逐帧刷新

音色库参考录音与合成音轨的播放进度改用 requestAnimationFrame 刷新，每帧读取音频 currentTime，不用帧数累加时间。暂停、播放结束、出错、切换音轨或离开页面时停止动画并清理监听；拖动进度后立即同步位置。

验证：Electron 检查逐帧进度变化与暂停后位置稳定，并回归播放、拖动、慢放及页面切换。

## 2026-09-24 11:00 · AK 改为本地明文存储

按用户要求移除 safeStorage 与系统钥匙串依赖，AK 明文保存在应用用户目录 qianwen-api-key.secret，文件权限 0600，继续排除 Git 和数据库/音频备份。页面说明同步更新，状态接口不返回密钥，输入框保存后清空。

旧版加密文件无需解密：设置中提示重新填写一次，新的明文密钥直接生效；无效输入不会覆盖现有文件，移除密钥会一并清除旧加密文件。

验证：14 项测试覆盖明文保存、重开读取、权限、旧密钥替换、删除与语音任务；Electron 检查保存/重开/移除流程。未调用真实付费 API。

## 2026-09-24 11:11 · AK 眼睛按钮与已保存密钥查看

在 AK 输入框右侧增加显示/隐藏图标。输入框为空时，点击眼睛可读取并显示当前已保存的完整 Key，方便核对使用的是哪个 Key；存在新输入时只切换可见性，不覆盖正在编辑的内容。当前仍是单个启用 Key，尚无多 Key 列表。

验证：Electron 检查已保存 Key 的显示/隐藏、新输入保留以及保存、重开与移除流程；测试使用虚构 Key。

## 2026-09-24 11:27 · 改善 AK 粘贴兼容与错误说明

兼容 AK 前后空白、边缘不可见字符、常见标签、Bearer 前缀和包裹引号；取消无依据的最短长度限制。内部空白、混入应用名称或多个 Key 等情况给出具体提示，不擅自拼接凭据。

验证：15 项测试通过，覆盖粘贴规范化及无效输入不覆盖已有 Key。用户已确认当前 Key 保存成功，本次未重启客户端或改写其密钥。

## 2026-09-24 11:30 · 多 Key 列表与刷新恢复

设置页改为真实 Key 列表，支持新增、命名、编辑、删除和选择当前使用的一项。默认显示完整内容，眼睛独立切换每行可见性；刷新和重开后恢复已保存内容，不再使用空输入框占位。

明文列表保存到应用数据目录 qianwen-api-keys.secret，原单 Key 作为默认条目读取并在保存时迁移。列表原子写入、拒绝重复 Key 和无效当前选择，删除全部后关闭云端，不从旧文件复活已删条目。凭据不写入数据库、常规状态或备份。

验证：16 项测试通过；Electron 检查两个 Key 的录入、命名、切换、显示隐藏、页面刷新、客户端重开及删除。所有测试使用虚构 Key。

## 2026-09-24 11:50 · 兼容 Qwen 返回的 HTTP OSS 音频地址

官方 Qwen-TTS 返回示例包含 HTTP OSS 地址，而旧下载校验只接受 HTTPS。现在对受信任的 aliyuncs.com 子域地址升级为 HTTPS，保留原签名查询串，再下载音频。其他不支持的地址提示协议和域名，不输出签名参数或用户凭据。

验证：17 项测试通过，包括模拟 HTTP 返回、实际下载参数使用 HTTPS、签名原样保留、音频保存、无凭据下载及异常地址拒绝。没有重新发起真实付费合成；此前失败任务未保存返回地址，不能直接恢复该次下载。

## 2026-09-24 11:54 · 云端 WAV 兼容与真实合成验证

兼容云端完整下载仍保留未知长度 data 头的 WAV，按实际 PCM 数据重写标准长度，不改变声音数据。继续拒绝普通 WAV 截断、样本帧不完整等情况。下载解析失败后保留本地文件，同一合成重试复用已有下载，成功后清理。

验证：19 项自动测试通过，覆盖未知长度头、PCM 一致性、实际截断和缓存重试不重复合成。用用户刚才失败的同一句及已有云端音色做了一次真实重试，合成成功并保存 3.68 秒音频；该次调用可能产生平台费用。未评估音色相似度。

## 2026-09-24 11:57 · 标注每次合成的模型

生成确认窗口显示本次使用的模型 ID；排队、生成中、成功和失败记录均显示该次任务保存的 model_id。历史音频按记录展示，不随当前设置变化，缺失字段时明确显示未标注。

验证：JavaScript 语法与差异检查通过。仅展示已有模型字段，没有调用合成接口。

## 2026-09-24 12:07 · 云端模型选择与独立音色缓存

设置提供默认云端模型，生成窗口可单独选择本次模型。支持 Qwen3-TTS VC 2026-01-22、Qwen-Audio 3.0 TTS Plus、3.1 TTS Flash 和 3.0 TTS Flash。Qwen-Audio 接通临时录音上传、音色创建和非流式合成接口；音色与音频按模型隔离，任务保存模型快照。

验证：23 项自动测试和 Electron 端到端测试通过，覆盖模型参数、上传失败、音色复用、单次覆盖、设置刷新恢复。Plus 实际克隆与合成成功，返回 3.68 秒音频；两款 Flash 已做模拟接口测试，尚未实际调用。

## 2026-09-24 12:20 · 全部 13 款克隆模型与独立滚动

补齐 CosyVoice 四款、MiniMax 四款及 Qwen3-TTS VC Realtime。按系列展示自定义勾选列表，用户选择默认模型；单次生成可覆盖或主动设为默认。模型、生成方式和生成时的音色选择均不使用原生下拉菜单。MiniMax 首次音色解锁费用在选择处说明。

修复网格内容撑高整个页面：右侧内容独立滚动，左侧导航与顶部栏不随页面滚动；模型列表、弹窗内容各自滚动，弹窗操作按钮保留。

验证：35 项自动测试通过，包括 13 款接口流程、实时事件结束/中断/超时/取消、模型隔离与默认值。Electron 验证模型选择、刷新保存和左右滚动隔离。此次未批量发起真实付费合成；真实验证范围仍为 Qwen3-TTS VC 与 Qwen-Audio Plus。

## 2026-09-24 12:35 · 模型列表常驻滚动条

模型列表右侧增加细线轨道与常驻滑块，不依赖系统自动隐藏滚动条。支持拖动滑块、点击轨道、键盘方向键和 Home/End；增加可见条目范围、上下翻动按钮与边缘渐隐，明确提示更多模型。保持列表独立滚动。

验证：Electron 完整回归通过，并检查滚动条键盘操作、轨道点击、滑块拖动与页面不跟随滚动。

## 2026-09-24 12:43 · 音量、字号与可见时间轴

录音样本、合成结果和裁剪试听增加统一音量滑条、百分比及静音按钮，音量偏好保存在客户端。顶部提供字号缩小、恢复与放大，范围 85%–130%，重开保留。波形下方增加可见横向进度轨道与时间刻度，保留波形点击跳转，播放位置继续用 requestAnimationFrame 更新。

验证：Electron 检查音量实际作用于播放器、静音切换、字号与音量刷新恢复、时间轴可见及播放跳转，原有录音、裁剪、存储和窄屏流程回归。

## 2026-09-24 12:51 · 顶部统一音量与字号滑条

将音量集中到顶部一个全局滑条，移除每条录音、合成结果和试听区的重复控件，继续统一作用于所有播放。字号调整改为小 A / 滑条 / 大 A，百分比按钮可恢复默认，保留持久化偏好。窄窗口压缩控件宽度。

验证：Electron 回归检查顶部唯一音量控件、实际音量与静音、字号滑条、偏好恢复及窄屏布局。

## 2026-09-24 12:53 · 播放圆点与波形游标对齐

修正原生 range 滑块中心只能在轨道内侧移动、SVG 游标却覆盖完整宽度造成的偏移。进度控件按滑块半径补偿两端，轨道保持波形宽度；取消进度步长取整。时间刻度固定在 0%、25%、50%、75%、100%，不再随文字宽度偏移。

验证：JavaScript 语法与差异检查，Electron 播放、暂停、跳转、窄屏布局回归。

## 2026-09-24 13:51 · 可复制、导出的完整 Skill

设置提供「复制完整 Skill」「导出完整 Skill」「打开 Skill 文件夹」。复制内容含全部说明、协议和脚本，可粘贴给其他客户端重建目录；导出完整文件夹，不包含用户密钥或数据。Skill 的本地模型检查、安装与注册依赖移入包内，原仓库入口保留兼容包装，复制后无需仓库路径。

修正接入文案：本地接口开关仅代表允许调用，不等于宿主已安装或已提交。显示实际评估回执或尚未收到回执。

验证：Skill 结构校验通过；37 项测试通过。将完整包导出到临时目录后，用独立 Node 进程完成 context 与 review 提交，验证回执和本地模型预检；没有向用户数据库写入测试评估。Electron 回归验证复制与导出按钮。

## 2026-09-24 18:09 · 我的录音与本地录音模型登记

新增录音会话、多文件导入、麦克风连续录音及约30秒分段保存；每个会话用一个 block_id 关联独立音频资产和可编辑转写。FSMN VAD 与 SenseVoice INT8 在本机完成切段和转写，Skill 负责下载和运行环境登记。CAMPPlus、Zipformer 可登记并独立验证，尚未接入说话人区分或关键词交互。已有 Qwen3-TTS 可继续复用。

验证：39项单元测试通过；Windows Electron 录音界面及原客户端回归通过；经授权的本机录音完成客户端导入、转写和保存，四项模型状态读回成功。CAMPPlus 输出192维有限特征，Zipformer 官方样本检出10次关键词。个人录音、转写内容、模型权重与机器配置不进入版本库。修正跨平台界面测试为设置内容区尺寸。

限制：录音本机保存，导入单文件限100MB，统一转PCM WAV；说话人聚类、关键词设置及后台监听未实现。转写结果需要人工核对。

## 2026-09-24 18:17 · 录音说话人候选分组与自定义关键词

录音会话增加手动分析入口：CAMPPlus 按 VAD 语音区间和3秒窗口进行会话内候选分组；Zipformer 支持最多20个中英文关键词。分析结果与片段分别保存，重新打开客户端后恢复，不覆盖录音或人工转写。单次最多100个片段，错误或会话变化不会留下部分分析结果。

验证：录音存储及输入边界测试通过；客户端真实音频导入、转写、候选分组、关键词检出和重启恢复通过。示例单人录音被分为同一候选组，两个指定词被检出。新增分词依赖纳入 Skill 安装清单。

限制：说话人分组为实验功能，0.65相似度阈值尚未针对多种真实会议评估；不能分离重叠发言，短于1秒的窗口标为不确定。关键词暂不显示精确命中时间，不开启后台监听。

## 2026-09-24 21:29 · 录音策略复盘：跑通不等于效果满意

本条为依据已有对话、代码和工具结果整理的回溯记录。时间是本次整理时间，不冒充各次反馈或实验发生时间。此前 18:09、18:17 的记录保留为当时状态；其 100 MB、100 片段等限制随后已有变化，不能作为当前能力说明。相关实现提交：ba08b53、a16c594、a775950、6fa9003。

### 用过什么策略，实际证明了什么

1. **固定约30秒保存音频。** 初版在客户端解码后转为单声道 PCM WAV，每约30秒保存一个片段，一个会话 block_id 关联多段音频。这样先跑通连续录音、多文件归档和恢复；它是存储分块，不是自然句子边界，也不是说话人切换边界。初版单文件100 MB、分析最多100段，后来代码改为保留原文件、配套 FFmpeg 解码、按自然停顿分段和顺序处理长录音。这里记录策略变化，不把这些限制都写成用户逐项投诉。
2. **FSMN VAD 按有声/停顿切段，SenseVoice INT8 转写。** 真实参考录音曾成功得到中文文字和语音时间范围。这只能证明该样例的本地流程可运行；VAD 检测语音活动，不能据此确定谁在说话。自然停顿也不必然等于一句话或换人。
3. **CAMPPlus 特征 + 3秒窗口 + 0.65余弦相似度阈值分组。** 我们实际采用的是候选聚类策略，不是已验证准确的会议说话人分离。单人样例的多段被归为同一候选组，模型输出192维有限特征；这不能证明多人数量正确、轮次边界准确，或复杂会议效果已达标。固定窗口可能跨越换人时刻；短于1秒的窗口标为不确定，重叠发言未分离，阈值未做多样本校准。
4. **Zipformer 声学关键词检测。** 官方样例检出10次关键词；另一真实样例检出两个指定词。仅代表这些样例成功，没有评估通用误报率、漏报率，当前界面没有精确命中时间。关键词模型不能代替说话人识别或全文转写。
5. **说话人分析与转写曾各自独立。** 单段和批量转写曾重新运行 VAD，没有使用用户确认过的说话人边界；此前批量入口还先转写、后分析说话人。即使两个模型都跑通，也不能得到用户需要的“谁在何时说了什么”的确认流程。

### 用户明确表达的不满意与纠正

- 用户指出“我的音色”和“我的录音”使用了重复图标，希望用磁带、文件夹或耳机区分。随后将录音入口改为细线磁带图标。代码已修改，不把这一修改写成用户已再次确认视觉满意。
- 用户指出音频处理还有问题，提出先检查有几个人、谁在哪些时段发言，再切分转成文字。英文中的部分词存在转写歧义，我们没有把“three times”解释成“三个人”，也没有把“to text”擅自认定为翻译请求。
- 经询问，用户明确确认：“对，先确认说话人和时间段，再转写”。这说明此前独立分析/直接转写的流程不符合他的要求。没有足够证据写成“模型已被实测证明数错了几个人”：当前反馈确认的是处理顺序和可核对性的问题，具体多人识别准确率仍待验证。
- 更早的线上英语回顾原型，用户确实反馈过合成音频杂音、排版太大、纵向堆叠、播放进度不清楚、播放卡顿和重播时界面抖动。后来对其中一些改动表达过满意。它们是线上原型的体验记录，不能据此声称本 Electron 客户端对应问题全部复现、全部修复或已获同等认可。

### 本次改正与未完成的验收

6fa9003 加入：候选识别后，用户逐段试听、修改开始/结束时间与说话人名称并确认；未确认不能转写。转写直接使用确认后的时间区间，不再重跑 VAD 改写边界；保存说话人、时间和文字。原录音及手工笔记保留，修改分段后旧文字标记待更新。人数根据确认后的名称统计，不等于自动识别真实身份或准确测量实际人数。

验证证据：50项自动测试通过；隔离数据的 Electron 测试中，使用经授权的真实录音确认两个测试区间，验证单段和批量转写保留指定边界与说话人、刷新恢复，以及未确认/未保存修改时禁止转写。测试里的名称和区间是测试输入，不是自动识别的真实人物结论。尚无用户对这版多人分段效果的实际验收，也没有多人基准集、边界误差或说话人分离错误率数据。

一次额外的长录音自然分段测试没有通过：脚本要求超过70秒并覆盖原30秒分块边界，但运行时误用了约18.8秒的参考录音，失败在时长断言。不能把这次失败隐去，也不能将其当作产品长音频故障或宣称该轮长音频回归已通过；需要用符合前提的长音频再测。

### 工程失败也保留

模型下载曾遇到 curl TLS 连接失败和 urllib 请求失败，后改用 requests，在正常证书校验下完成下载；没有以关闭证书校验绕过。FSMN 首次推理因导出文件名/配置字段与 funasr-onnx 要求不一致失败，适配 config.yaml、am.mvn 和配置字段后成功。Windows 子进程中文输出曾因编码不一致无法解析，显式 UTF-8 后复测成功。Electron 测试曾因窗口尚未建立就调用 hide 失败，改为等待窗口；另一次因窗口外框尺寸不等于内容区尺寸超时，改用 setContentSize。它们分别是安装、适配或测试问题，不归因于用户操作，也不包装成模型质量提升。

后续仍需验证：多人交替与重叠发言、不同距离/噪声条件、候选人数与时间边界、长录音跨分块连续性，以及用户确认步骤是否方便。继续记录不满意和失败，不能只追加成功项。

## 2026-09-24 21:34 · 本地说话人拆条与转写策略复现

新增「分人语音条」完整流程：通过系统选择器导入长音频，或处理已有录音会话，使用本地VAD、CAMPPlus、稳定窗口聚类和SenseVoice生成最长18秒的短语音条。支持语言、自动/指定人数、进度与取消，结果另存会话，保留原音频和人工修改。列表按说话人筛选并分页，减少同时挂载的播放器。

- 将102分钟访谈试验中的策略整理为独立Python worker与文档，修正异常短声导致直接全量聚类失真的问题；不足以判断的极短语音保留为不确定，不强制借用邻近标签。
- 新增受限IPC、临时目录、worker进程组取消、结果校验、原始revision保护和整批事务；失败清理新文件，不留部分会话。
- 模型路径由runtime登记，支持Silero优先/FSMN回退；提供独立依赖清单、复用已有模型的登记工具及SHA256/版本记录。没有把模型、访谈音频或转写内容加入仓库。

验证：44项Node测试、4项Python算法测试通过；旧录音桌面流程回归通过。整段约102分钟音频在新worker中输出648条、两个主要说话人及38条不确定语音，整批入库通过。独立CPU环境的120秒双人样本通过真实Electron IPC生成13条，并验证筛选、播放、重开恢复和取消不产生部分会话。测试截图曾因隐藏窗口超时，已改为显式可选，不影响上述功能检查。

限制：声音标签和文字均为未人工校对的机器结果；换人点近似、重叠声音未分离。旧会话30秒原始边界保留，自动人数为启发式。当前验证macOS CPU，未宣称手机端或其他桌面平台已验证。

## 2026-09-24 21:45 · 合并远端录音确认流程与本地分人拆条

拉取远端 main（b5f1550），保留会话列表、名称/文件搜索、原音频归档、确认时间段后转写及完整策略复盘记录。逐处合并主进程、preload、录音页面和文档冲突，没有覆盖远端确认流程。

客户端分人入口改为先生成候选短语音条，试听、编辑并确认后才转写。原文件与新结果原子保存，候选标签预填到确认编辑器；过滤和40条分页仍保留。独立命令行复现实验保留自动转写，客户端固定关闭自动转写。确认后的转写只依赖 SenseVoice，移除未使用的 FSMN 导入；保留语言选项和确认后的精确区间。

验证：55项Node测试、4项Python算法测试通过。隔离Electron真实双人样本生成13条，验证未确认禁止转写、测试区间确认后实际转写、原文件归档、筛选、播放、重开恢复及取消。远端确认流程测试验证单条/批量转写保留编辑边界和说话人，未保存编辑禁止转写。正式应用已重启并打开「我的录音」新入口。

未通过的检查：远端 smoke-recordings 在没有设置完整FSMN runtime的隔离环境运行，导入提示缺少FSMN，随后等待导入完成超时。本机最小运行环境仅配置Silero/CAMPPlus/SenseVoice，新分人流程已验证；该轮不能宣称旧FSMN自然分段、关键词或完整旧录音界面回归通过。没有为合并隐去此前失败记录或把机器分组当作用户验收。

## 2026-09-24 22:02 · 对话记录界面、说话人备注与会话时间

根据用户反馈，将录音片段呈现为聊天记录：每段显示原文件时间范围、声音标签、播放器和下方转写文字。确认分段、修改文字及音频处理工具折叠，仍保留先确认再转写的行为。新增会话级说话人列表，支持备注名和说明，修改后同步到全会话、筛选与 transcript.txt 导出；别名不改写原始分组、确认区间或识别内容。人工修订正文优先于旧机器分句，避免出现两份不一致的文字。

会话列表和详情显示创建时间、最后更新时间；最后更新取会话、录音片段和原文件记录中的最新时间，覆盖转写、确认分段和备注操作。精确音频范围显示到毫秒，多个原文件分别保留来源名称。每页40条，避免长访谈同时挂载大量播放器。

验证：57项自动测试通过；隔离Electron测试覆盖43条样例、说话人备注、筛选、跨页同步、人工修订、重开持久化、实际文本导出、会话时间及680px窄窗口，桌面/窄屏截图已检查。真实模型确认流程回归通过，保留单条和批量的精确时间与说话人。测试曾遇到折叠控件不可点击及隐藏窗口截图超时，调整测试为先展开控件、截图时显示隔离窗口后通过。没有向正式会话写入测试备注或虚构转写。

## 2026-09-24 22:12 · 说话人勾选确认与紧凑的中性色对话列表

用户要求不确定片段可新增说话人，或勾选本会话已有的一位/多位再确认。时间段编辑器改为复选项；新增名称选中后统一确认保存，空选明确报错，“暂不确定”与具体说话人互斥。多人保存为独立标签数组，转写、筛选、人数统计和文本导出保留各自身份。会话记录已出现的说话人，暂时没有片段的标签仍可再选；备注名独立于底层标签。

依据用户提供的界面参考，将彩色大气泡改为按原音频文件折叠分组、紧凑的说话人/时间/正文/操作行。使用白色、浅灰、细边框和低饱和头像，加入逐段播放、进度和复制文字。保留创建/更新时间、已有确认流程和文本导出；未实现参考图中的翻译、分享或全文件波形，这些不作为已完成能力展示。

验证：58项自动测试通过；隔离Electron确认已有/新增/多人选择、跨页及重开保存、备注与导出、桌面和680px窄屏布局。真实模型回归保留已确认时间和说话人，单条及批量转写均通过。截图已检查；结果不代表用户已验收或说话人识别准确率已评估。

## 2026-09-24 22:22 · 参考布局第二版与双视图页签

进一步贴近用户提供的布局：顶部采用头像组和折叠说话人备注区，文件分组下使用紧凑对话行，编辑操作收进每行菜单。新增「紧凑视图 / 对话视图」页签，可鼠标或左右方向键切换，共享当前播放器、分页与输入内容，不重建表单、不丢失未保存文字。

验证：58项自动测试通过；隔离Electron验证两种视图切换、键盘切换、未保存草稿保留，以及原有说话人选择、备注、导出与播放。两版桌面截图及窄屏布局检查通过。

## 2026-09-24 22:39 · 按参考图重排紧凑文件与片段列表

用户要求严格按参考图的构成重做紧凑版。会话头展示说话人和音频统计，添加音频为主按钮；每个文件包含序号、时间、时长、说话人、播放和折叠入口。展开后用头像、姓名、时间、正文、操作和编辑六列排列，默认展示六条并可展开本页其余片段。保留对话 Tab 和未保存草稿。

波形使用导入时保存的真实峰值，按原始时间偏移绘制，支持缩放与原文件定位播放。无整段原文件时明确显示“顺播本页”。原方案的浏览器音频读取检查发现部分波形不可用，已改为直接使用已保存峰值。界面仍使用现有转写功能，不把未实现的翻译或分享做成可用按钮。

验证：58 项业务测试通过；Electron 交互检查覆盖六条展开/收起、双视图草稿保留、说话人别名、多选确认、播放、导出、分页和窄窗口。真实内容的显示效果待用户验收。

## 2026-09-24 22:49 · SVG 波形缩放与播放位置竖线

用户发现 Canvas 横向拉伸后模糊，并指出缺少当前播放位置的竖线。改用 SVG 固定线宽矢量路径，窗口尺寸或缩放变化时重新计算显示柱数；保存峰值采用加权平均聚合，避免每个大区间取最大值造成近乎等高的柱子。放大只展示已有峰值的细节，不生成虚假的音频细节。

新增播放位置竖线，随音频逐帧更新，拖动、点击跳转和缩放后保持时间对齐。增加观察器及动画的清理，避免切换会话后遗留更新。业务测试共 59 项通过；Electron 检查扩展至 SVG 缩放、固定线宽和播放游标。

## 2026-09-24 22:57 · 独立波形时间轴、进度条间距与说话人头像

波形滚动容器保留横向滚动，仅隐藏滚动条外观并固定高度；整段进度条移出滚动容器，增加间距与“整段进度”标注。SVG 时间刻度按缩放级别选取间隔，横向滚动时展示对应原始时间，明确局部波形与全局进度的关系。

说话人支持本地 PNG/JPEG/WebP 头像，选择后居中裁切至 192 像素，预览并随备注一起保存；可恢复文字头像。会话顶部、文件头、说话人目录和发言行共用头像。存储层限制格式与大小，修改备注时保留头像。60 项业务测试通过，包含头像保存、保留和移除。

## 2026-09-24 23:06 · 连续发言共用吸附身份栏

紧凑视图中同一说话人的相邻片段共用头像和姓名，滚动时身份栏在当前连续发言组内吸附，下一位说话人开始后替换。一人一句、未知身份、多人标签以及序号不连续的筛选结果不合并。各片段仍保留独立时间、文字、播放与编辑。

分组后保留六条预览、展开更多、分页和对话 Tab。窄窗口保持逐条身份，避免吸附栏遮挡正文。Electron 检查增加连续组存在及滚动吸附位置断言。

## 2026-09-24 23:20 · 语音文件模式复盘、README 汇总与识别优先级

这是依据对话、代码和测试结果补写的阶段复盘，时间为整理时间，不冒充每个决定的发生时间。用户要求把功能写进 README，也要记录思考、犹豫、反复和选择，并展示有来源可核对的技术 Logo。随后明确补充：语音识别出的内容优先，翻译晚点再说。

### 我们要解决的事情，如何逐步变得清楚

起点是检查本地 Freenote 类应用的技术与小模型策略，以及“手机能否独立跑”的疑问。随后需求落到一份长访谈：按不同的人拆开、拆成短语音条、识别成文字并标注说话人，再复现进 SayAgain。手机端可运行性始终只是待评估问题，没有因此宣布完成手机部署。我们逐渐把目标从“模型能输出结果”收敛到“用户能知道谁在何时说了什么，并修正它”。

用户喜欢 transcript.txt 的原因很具体：能看到从什么时候到什么时候、能分段、能给 speaker 备注，并让显示随备注改变。后续要求创建 / 更新时间、已有说话人多选确认、参考图的紧凑构成、头像、波形刻度和连续发言吸附，都是在补齐同一份语音文件的阅读与校对体验。

### 反复过什么，为什么改变

| 问题或备选 | 实际反馈 / 观察 | 选择与仍然存在的代价 |
| --- | --- | --- |
| 一次自动拆分并转写，还是先让人确认 | 独立复现实验能自动输出文字；用户明确要求先确认说话人和区间 | 客户端先保存候选，再确认、再识别原文。增加一步操作，换来可核对的归属；CLI 自动模式保留用于实验，不绕过客户端规则。 |
| 全量聚类是否足够 | 极短异常声音会把聚类拉成“一大簇加一个异常片段” | 稳定窗口建中心，短句后分配，极短语音保留不确定。自动人数仍是启发式，不能当身份识别或准确率承诺。 |
| 直接填名称，还是从已有身份选择 | 用户要求不确定时可新增，也可勾选已有一人 / 多人 | 标签数组与备注名分开保存，确认才生效；多人不是新建一个拼接姓名的身份。 |
| 聊天气泡还是紧凑表格式阅读 | 用户先提出聊天记录式 UI，后来要求按照参考图再做一版并能 Tab 切换；首轮“贴近”还不够，要求严格按构成 | 保留双视图，紧凑版按文件头、波形和对齐列重排，共用数据与草稿。没有把参考图里未接通的翻译 / 分享按钮伪装成已实现。 |
| 靠 CSS 拉伸 Canvas 是否够用 | 用户截图指出放大模糊，且缺当前帧竖线；大区间最大峰值又让柱子近乎等高 | SVG 固定线宽、按缩放重新聚合已有峰值，补上逐帧播放竖线。清晰度改进不等于新增采样精度，原有峰值分辨率仍是上限。 |
| 波形滚动和整段进度是否能放在一起 | 用户指出滚动条影响高度、圆点进度条贴得太近，希望缩放时有时间轴区分上下状态 | 保留横向滚动但隐藏滚动条外观、固定高度；波形自带局部刻度，整段进度移出滚动容器并加间距。 |
| 每条都重复头像，还是合并身份显示 | 用户指出连续同人发言重复太多，一人一句则可以逐条 | 相邻同人发言共用 sticky 身份栏；换人结束吸附，不跨不连续筛选结果合并。窄窗口仍逐条显示，优先避免遮挡。 |

这里的“犹豫”指可观察的方案权衡，不编造没有发生过的讨论或主观心理过程。旧实现、用户继续指出的问题和未通过的测试记录仍保留；测试通过不能代替用户对识别质量和整体体验的验收。

### 下一步选择：先补好识别原文，翻译延后

用户的最新优先级是 **语音识别原文 → 按人 / 时间校对 → 回放与整理 → 再考虑翻译**。当前 ASR 与确认流程已有实现，但真实会话中仍有未转写条目；不能因为 UI 做好就声称这些文字已经生成。后续优先关注确认后的识别入口、任务反馈、原文在发言下的呈现、错误与待校对状态，以及音频和文字一一对应。保留先前授权的确认规则，不为了填满页面而自动确认身份或生成示例文字。

期待更好的语音文件模式，是把原文件、连续发言、短语音条和可校对原文组织成稳定的阅读工具。播放跟随、区间循环、断点恢复、语义章节和更复杂的分离模型都可以评估，但不把这些方向写成已经实现；翻译本轮不推进。

### 本轮文档与发布状态

README 汇总现有录音功能、依赖路径和验证边界，分开本地语音识别、旧 FSMN 录音分段及语音合成，测试数量更新为 60。技术标识以本地文件保存，记录来源 / 版本 / SHA-256；没有独立标识可核对的项目使用文字链接，不把模型结构图当 Logo。未加入用户头像、访谈音频、真实转写内容或数据库。

前一轮实现已提交为 53012fc 并推送 main；拉取时远端无额外提交，无需制造空合并。本文档记录不把之后尚未进行的提交提前写成已完成。

## 2026-09-25 00:09 · 统一音频入口、先识别原文与连续滚动阅读

用户明确改变了此前的顺序：只保留分人语音条，直接录音也使用这一解析流程，先生成识别文字，之后再改说话人和时间。此前合并远端时选择“确认后转写”，是为了让识别区间服从人工边界；实际使用中，面对数百条空白语音，确认步骤阻挡了阅读。现在取消这个前置门槛，但没有把机器候选伪装成人工确认。历史日志保留旧决定，这条记录明确取代当前流程说明。

- 文件选择统一到分人流水线，移除普通导入、按停顿重分段、独立说话人/关键词分析的界面与 IPC 入口，旧数据仍可读取。
- 麦克风仍每约30秒保存，保护未完成录音；停止后仅处理本次素材。同次连续保存块拼接再检测，不把保存边界当发言边界。保留原块、完整拼接音频与派生结果；失败、取消不会覆盖旧记录。
- SenseVoice 自动识别原语言；单条/批量重试无需先确认身份。人工文字不会被批量覆盖，编辑中的时间段仍需保存后再重新转写。短语音缺少有效声纹时保留不确定标签，继续尝试识别。翻译继续延后。
- 用户随后反馈分页和展开更多打断长访谈阅读。改成文件内完整滚动列表，提供240–1200像素高度设置、侧边纵向滑块、百分比/当前条目范围和回顶按钮。保留连续发言身份吸附及紧凑/对话 Tab；音频不预加载。此版尚未实现万条规模的虚拟列表。
- 验证：真实60秒访谈文件自动生成7条带识别文字的候选语音；模拟硬件采集回调、实际运行 IPC/FFmpeg/本地模型，两个30秒保存块自动拼接并生成6条语音。两条路径模型选项不同，条数不作为准确率指标。62项 Node 测试通过；隔离 Electron 界面验证43条连续滚动、拖动到底、回顶、高度调整、吸附、备注头像、编辑、导出与窄屏布局。未宣称物理麦克风或通用识别准确率通过验收。

## 2026-09-25 00:16 · 修复内联列表无法滚动与滑块失效

用户实际使用发现“滚不了、拖不动”。上一版将初始高度放在模板的 style 属性，违反客户端 style-src self 的 CSP；高度未生效，内容撑开内部视口，导致没有可滚动距离，滑块被禁用。手动改高度使用 CSSOM 可以生效，但刷新或重新渲染又会复发。

修正为外部 CSS 提供520像素默认高度，绑定列表时通过 CSSOM 应用用户保存的高度，不修改安全策略。明确可滚动元素的最小高度，避免内容撑开布局。此前检查直接设置 scrollTop、给 range 赋值，没有先断言真实溢出；零滚动距离也会满足“已到底”检查，结论不充分。回归改为先断言初始高度、实际溢出和滑块可用，再通过鼠标滚轮和按下/移动/抬起拖动滑块验证；同时检查保存高度重载后仍生效，将 CSP 样式错误纳入失败条件。

## 2026-09-25 00:23 · 底部拖动调节列表高度

用户希望在列表底部直接推拉调整高度，而不必回顶部输入数字。将底部状态栏变成整条可拖动区域，中央双线标记提示用途；上下拖动实时改变高度，同步顶部数字并记住设置。使用指针捕获保持拖离区域后的连续操作，按显示缩放换算位移；支持方向键每次20像素、Shift加速与Home/End边界。高度仍限制240–1200像素。补充实际鼠标拖动、数值同步与键盘调节的桌面回归检查。

## 2026-09-25 07:20 · 会话成为容器，文件与录音在内部追加

用户指出保存策略偏离文档列表预期：在一个会话内添加文件、继续录音，应成为同一文档中的多份音频，不应不断派生到外层，并叠加“分人语音条”后缀。此前用独立结果会话保护原始资料，但把数据保护和用户的文档组织混在了一起，造成重复会话与上下文断裂。本次改为在同一事务中向当前会话追加音频来源和解析片段，保留旧内容；列表导入才创建新会话。

- 会话内导入携带目标会话 ID，录音停止后同样落在当前会话。标题、已有片段文字、人物备注与头像不重写；每份音频有独立来源 ID，同名文件也不串用原文件播放器。
- 成功解析后仅将本次原录音保存块归档，文件资产和手工笔记保留，避免原块与拆条重复展示、时长重复统计。失败、取消或版本冲突不提交半份结果。片段顺序从所有已用序号之后追加，避免归档后再次录音发生序号碰撞。
- 不同解析任务的 A/B 不代表同一人，追加时分配不冲突的候选标签并保留机器原标签，防止误继承旧人物头像/姓名；人工仍可选择已有说话人。
- 会话中的重处理入口限定为“解析待处理录音”，不再把已经解析、校正的整个会话重复复制出去。历史上已经拆出的独立会话保留，不猜测归属后自动迁移。
- 回归覆盖同一文档追加两份同名文件、旧人工文字和人物备注保留、原录音块归档与资产/笔记保留、序号递增；实际模型的桌面导入和模拟麦克风停止流程验证会话 ID 不变。

## 2026-09-25 07:37 · 可复用人物库与本次 Speaker 多头像

用户希望处理多个音频时，能明确指认“这个 Speaker 就是另一个人”，以后通过搜索引用人物库；随后补充，同一人物在本次音频可以用不同头像，并将多张头像留在人物库。

选择把机器声音标签和人物身份分开：保留每份解析的原始标签，以显式 person_id 引用人物。可以搜索库中姓名/备注，选择当前会话另一标签（尚未入库时一起建库），或用上方资料新建人物。既不通过同名推断同一人，也不按声纹自动跨文件认人。解除引用保留当前可见姓名、备注与头像。

姓名和备注属于人物共享资料；头像分为人物默认头像、去重头像集合和 Speaker 局部覆盖。新上传头像只应用到本次 Speaker，并入库；其他会话不跟着换。头像库可点选后保存，也可恢复默认头像。引用和人物编辑校验两个实体的版本，避免旧窗口覆盖新资料。人物保存在本机数据库，未引入云端同步。

67项 Node 测试通过，覆盖跨会话引用、重开持久化、资料同步、独立头像、头像积累、恢复默认和解除引用，以及无效/过期操作回滚；桌面回归加入搜索指认、本次图片上传和图库重开验证。

## 2026-09-25 08:01 · 修复人物关联“记录内容过大”

用户上传的真实头像触发了人物关联失败。根因是上一版把头像 Base64 放进人物默认图、头像数组和会话局部覆盖，重复累加超过单实体256 KiB上限；此前界面测试使用小纯色头像，未暴露实际图片体积问题。

改为本地内容寻址图片文件，人物/会话只存短引用，同图跨字段去重。读取兼容已有内嵌头像，下一次相关保存时转换，不要求用户重传，不调大数据库限制。恢复默认、局部覆盖与图库含义保持不变，备份包含图片文件。

68项测试通过，包括生成192×192随机RGBA的有效PNG（约190 KB Base64）、复现旧内嵌头像、创建人物引用、第二张头像、多次选择去重、记录体积和备份恢复。更新旧测试以验证读取后的显示头像而非要求数据库继续嵌入 Base64。

## 2026-09-25 08:38 · 方形头像裁剪与纵向播放标记

用户希望自行选择头像照片的取景，明确要求保留 400×400；同时指出滚动列表后，波形上的播放游标不能说明当前播放片段在列表哪里。此前自动居中裁成 192×192，无法调整人物偏离中心的照片，因此改为上传后弹出方形画布，拖动、缩放或方向键调整，确认后输出 400×400 PNG；取消不改原头像，保存备注后才持久化。原始上传照片不另存，旧头像不自动放大。

图片已独立文件存储，但此前仍遗留 400000 字符的图片输入校验；400×400 无损图片可能超过此值，本次同步调整为 2 MiB 的 data URL 上限，数据库单记录 256 KiB 限制保持。选择保留 PNG，避免重复有损编码。

右侧增加红色横线，按播放时间映射至对应片段在完整列表中的实际位置；灰色滑块继续表达浏览位置。整段播放和单条试听都更新，暂停保留，空隙映射最近片段，不强制滚动打断阅读。两种位置分开显示是用户本次明确要求。

验证：68 项 Node 测试通过；桌面冒烟覆盖裁剪输出尺寸、人物头像保存、播放标记和原有滚动拖动交互。

## 2026-09-25 08:47 · 裁剪预览增加圆形显示范围

用户指出方形裁剪结果实际显示为圆形头像，单看方框无法判断边角是否会被遮掉。本次在方形画布之上叠加正圆镂空遮罩，圈外半透明变暗，旁边增加实时圆形缩略图；拖动、缩放和方向键调整时同步。遮罩不拦截拖动，也不写入保存图片，输出仍为 400×400 方形 PNG。

复用桌面冒烟检查遮罩与缩略图可见、裁剪交互及保存尺寸，并保留裁剪弹窗截图检查布局。README 同步描述。

## 2026-09-25 08:55 · 校准纵向播放标记并逐帧更新

用户继续反馈红线需要考虑 scrollHeight，尤其没有滚动时应响应容器高度，并明确要求 requestAnimationFrame。复查发现旧实现把所有列表都压缩到较短的滑块轨道，轨道两端还预留回顶按钮和百分比区域；短列表因此不能与当前片段水平对齐，且只监听行尺寸遗漏了容器高度变化。

改为两种坐标映射：溢出列表用内容坐标 / scrollHeight 映射轨道；无溢出列表用片段当前进度的实际屏幕坐标对齐红线。红线移到整个侧栏定位，统一换算字号缩放后的屏幕坐标与 CSS 像素。观察列表、侧栏、轨道和行的尺寸，滚动、展开、暂停拖动进度也调度更新。

播放时 requestAnimationFrame 连续读取当前音频时间；暂停后停止循环，布局变化只请求一帧，页面重绘时取消旧帧和观察器。增加短列表无溢出及 900→600 高度变化后的对齐回归检查，保留长列表末段映射与浏览滑块不跳动检查。

## 2026-09-25 09:15 · 同人物默认头像合并展示

用户发现 Speaker D / E 都已明确指向同一个人物、没有设置独立头像，文件头与连续发言仍然重复显示。此前吸附分组只比较机器标签，这与人物引用后的阅读预期不一致。

引入仅用于展示的身份键：有 person_id 且无 avatar_override 时使用人物身份，否则使用原 Speaker 标签。会话头、文件头按展示身份去重，连续且原序号相邻的片段可共用身份栏。独立头像即使图片相同，也仍按各自 Speaker 显示；姓名相同但未关联人物不自动合并，未知 / 多人标签仍不合并。用户随后指出文件头的加号容易误解为新增，改用右箭头进入说话人列表，并补充“查看与编辑说话人”提示；省略号留给右侧音频操作菜单。人物库编辑、筛选、原始标签、语音条与转写内容不变，解除引用或恢复默认头像后展示重新计算。

验证：69 项 Node 测试通过，覆盖同人物、不同人物同名、未关联、独立头像与解除引用；桌面回归检查关联后恢复默认头像合并、设置独立头像重新分开，以及语音条数量与编辑卡片保留。

## 2026-09-25 09:20 · 复制图标与就地反馈

用户将紧凑列表中的空心方块理解为勾选框，实际这是复制文字按钮；此前使用字符 ▢ 表意不清。本次替换为两张纸叠放的描边 SVG，并补齐“复制文字”的悬停提示和无障碍名称。用户进一步要求点击后有提示，因此剪贴板写入成功后按钮变对勾，紧凑视图旁显示“已复制”，约 1.8 秒后恢复；对话视图原文字按钮同步显示“已复制”。失败仍报告错误，不展示成功状态。

## 2026-09-25 09:46 · 可切换的跟随播放

用户希望在“整体播放”旁增加一个小图标按钮，让纵向列表自动跟随播放红线。新增定位框与播放三角组合的 SVG 图标，带 aria-pressed 与开启 / 关闭提示，开启状态使用深色背景。默认关闭，每份音频独立切换，当前页面内重绘保留开关状态。

复用红线的 requestAnimationFrame 更新和内容坐标，将当前播放位置移到列表中部并限制在实际可滚动范围内；无需滚动时保持原位。支持整段、顺序和单条播放，暂停时不启动持续动画。点击开启即定位当前进度，不擅自开始播放。手动滚轮、触摸滚动、键盘浏览、拖动纵向滑块或回顶时退出跟随，避免与用户阅读操作争抢位置。

桌面回归覆盖开启后定位末段、关闭后不自动滚动、手动浏览解除跟随，既有红线与列表高度检查保留。README 同步。

## 2026-09-25 09:51 · 纵向轨道按圆点中心校准

用户指出跟随时红线与圆形滑块错开，需要在起止两端预留半径。检查发现两个原因叠加：红线使用整条轨道外沿，而滑块的圆心只在扣除两端半径的区间移动；红线按 contentY / scrollHeight，滑块按 scrollTop / (scrollHeight - clientHeight)，两种比例本身也不同。

统一使用当前片段居中时的目标 scrollTop / 最大可滚动距离，并限制在合法范围；跟随滚动复用同一个目标值。纵向滑块明确定义 16px 圆点，红线取实际滑块控件边界，并按显示缩放换算上下 8px 预留，以圆心为基准。无滚动时仍对齐当前片段实际高度，沿用 requestAnimationFrame。

回归增加跟随到末段时红线与滑块中心的坐标检查，保留真实拖动滑块到末尾、短列表、动态高度及手动退出跟随验证。

## 2026-09-25 10:05 · 人物形象、波形颜色与日期时间轴

用户提出 SVG 波形颜色可选择，人物有默认色、每次 Speaker 可单独覆盖，并指定左侧导航为“表达回顾 / 我的音色 / 我的音频 / 人物形象”。本次将人物库从 Speaker 内的引用选择扩展为独立管理页，可搜索、新建、编辑姓名备注、裁剪上传默认头像、选择已有头像和设置默认颜色。原“我的录音”改为“我的音频”。

颜色以人物默认 color 与本次 color_override 分层存储，未关联的 Speaker 也能设置本地颜色；默认变更传播到未覆盖的引用，本次独立色不受影响，可一键恢复继承。SVG 按最终颜色合并路径，不再固定三种颜色；关联同一人物且使用默认色的不同声音标签显示同色。独立颜色不改变上一轮基于人物与独立头像的合并展示规则。六位十六进制校验避免把任意字符串带入 SVG；人物编辑继续使用 revision 检查。

用户随后给出日期时间轴参考图并要求排序与统计。询问应用范围后，尚未收到选择，先按“我的音频 / 人物形象都加”的假设实现：按本地创建日期聚合数量，选择日期过滤，最早 / 最新排序，搜索同步统计。时间轴统计对象分别为会话与人物，人物引用统计另显示会话数与声音标签数；不是按音频内的秒数统计。

验证：71 项 Node 测试通过，覆盖跨会话默认颜色传播、本地覆盖及重置、重开持久化、非法颜色与过期版本，以及人物页面新建 / 默认头像变更。桌面检查覆盖人物管理、搜索、颜色修改反映到 SVG、覆盖不被默认变更替换；新增日期筛选与排序检查。先前冒烟硬编码“三种路径颜色”的断言在动态配色下失败，已按实际可用颜色行为调整。README 与数据模型同步。

## 2026-09-25 10:12 · 表达回顾补齐日期时间轴与排序

- 用户明确补充：表达回顾也需要时间轴与排序。上一轮按推断只接入我的音频和人物形象，覆盖范围不足，本轮补齐表达页。
- 复用同一个日期统计组件，按本地创建日期统计表达数量，支持全部日期、单日筛选、最早优先与最新优先。搜索、语言、收藏和归档条件先筛选，再统计日期；顶部显示数量包含日期筛选。
- 保留现有表达卡片和语音练习流程；切换日期或排序时停止旧卡片播放，避免播放器引用已移除的节点。
- 验证：Electron 桌面烟测通过，新增验证日期按钮、正逆序、搜索统计和收藏统计；现有语言筛选、窄屏、播放和重启持久化验证仍通过，并检查桌面截图。README 同步。

## 2026-09-25 11:08 · 表达回顾搜索吸顶与回到顶部

- 用户在长列表浏览时需要持续使用搜索，并能一键回到起点。将表达页搜索和筛选工具栏设为 sticky，定位于 main 滚动容器顶部，保持在应用顶栏下方；白色背景防止内容透出干扰输入。
- 右下角增加带提示和无障碍名称的回顶图标按钮，操作 main 的滚动位置，默认平滑滚动；系统设置减少动画时直接回顶。按钮仅在表达回顾显示。
- 验证：Electron 桌面烟测通过，实际滚动后断言搜索栏与滚动容器顶部对齐，并点击按钮确认 scrollTop 回到零；原有搜索、时间轴、播放与窄屏流程继续通过。README 同步。

## 2026-09-25 15:35 · 右上角语音生成队列

用户希望多条语音生成时能集中看到逐个进度，并建议右上角时间图标。沿用现有串行生成机制，新增时钟入口与待处理数量；展开显示后台实际排队顺序、当前项已用时间、完成、失败原因和取消操作。当前生成接口没有可靠剩余时长，因此显示真实已用时间，不伪造倒计时或百分比。播放音频或编辑表单期间仍单独更新队列；保留最近20条终态记录。

增加排队、开始、结束时间；重试会重置计时。语音业务26项测试通过，隔离 Electron 队列检查覆盖空状态、顺序、计时、数量、失败、文本转义、键盘关闭与窄窗口。完整桌面回归也已通过（生成提示文案断言随本次文案更新）。真实模型耗时预测未实现；需重启当前应用加载主进程变更。

## 2026-09-25 15:54 · Skill 持久启用范围与跨对话接续

用户指出仅在本机说明里记录偏好不足以支持其他用户或跨工作区使用，要求把持久启用流程写进 SayAgain Skill 并提交 main。新增 references/persistence.md：明确本次对话、指定工作区、全局三种范围；按已授权选择合并实际生效的宿主说明，提供可移植模板，保留原有内容并去重。说明 Codex 全局/项目入口、override 优先级、多个独立 Git 仓库的路径限定入口，以及变更范围和暂停时的处理。

技能入口、连接文档、完整复制包引言、设置文案和 README 一并更新。持久偏好保存、新对话实际加载、连接成功和评估回执分开报告，不把安装当成每轮触发保证。不把开发机个人路径或凭证写入可移植包。

验证：技能格式校验通过；完整包导出与粘贴还原的所有文件逐一比对、Markdown 引用可达、隔离本地接口连接与提交测试通过。默认校验 Python 缺少 PyYAML，改用已有环境完成，未安装新依赖。未替用户选择全局/工作区持续范围，也未声称所有宿主的新会话触发已经实测。

## 2026-09-25 16:10 · 多说话人音频标题栏挤压修复

用户反馈 Windows「我的音频」文件名逐字竖排、头像铺满标题栏。隔离 Electron 数据以36个候选说话人复现：1280像素窗口下文件名宽度降为0、标题栏高度1412像素、播放控件溢出439像素。问题为无上限头像列表挤压 flex 标题，并非检测到 Windows CSS 不支持。

文件标题栏改为最多3个头像与剩余人数入口，仍可打开完整说话人目录；长文件名使用省略号和完整名称悬停提示，播放、跟随、操作按钮不被挤压。隔离布局检查通过紧凑/对话两种视图、7种窗口宽度、100%/130%字号及36位说话人目录可达性，并检查宽窄窗口截图。原有录音阅读器回归也通过，覆盖别名/头像、筛选、滚动/高度、播放标记、文本修正持久化和导出。未修改用户录音或说话人数据。

## 2026-09-25 16:16 · Windows 导入音频缺失 FFmpeg 路径

用户在「添加音频文件」遇到 [WinError 2]。本机录音 runtime 未配置 ffmpeg，分人导入流程退回系统命令，而 Windows PATH 中没有 FFmpeg；通过实际录音 Python 调用裸命令复现相同错误。应用 ffmpeg-static 程序存在，旧导入入口已有路径解析，分人流程却未复用。

统一复用 FFmpeg 路径解析：环境指定、已登记路径、应用自带程序依次选择；启动 worker 前检查文件有效性，缺失时提示具体程序路径而非晚到的 WinError。新增无 PATH、中文空格路径实际解码和无效配置不启动任务/不写数据的测试；导入与分人流程14项测试通过。

本机另在备份 runtime 后只补 ffmpeg 绝对路径，保留所有模型与验证信息，当前进程下次导入即可读取。实际登记的 Python 调用自带 FFmpeg 解码1秒隔离样例为16kHz单声道通过；本次未重复执行长音频分人/转写，也未修改用户录音。

## 2026-09-25 17:30 · SenseVoice 模型对照与 PatchX Freenote 展示预留

按用户提供的 SHA256 校验 Mac 模型压缩包、模型、词表与现有英文样例后，在同一 Windows、现有 sherpa-onnx 1.13.8、CPU、4线程、use_itn=True 下运行两份模型 × auto/en 四组。Windows 原模型输出全大写且无句末标点；Mac 提供模型输出正常大小写、句号及数字50。每份模型的两种语言设置结果一致。直接保留 stream.result.text，无文本后处理；推理耗时约0.317–0.356秒。完整路径、文件指纹与输出见 docs/sensevoice-model-comparison.md。

本次样例证明只改变模型文件就能改变大小写与标点，不能据此评价整体准确率或确定上游版本。Mac 来源按用户说明记录为 PatchxNote 的 patchnote-standard 0.2.0；两份模型均保留，原始文件与 runtime.json 校验未变，未切换配置或升级依赖，等待用户决定是否切换。

用户要求为 PatchX Freenote 留硬件录音广告位和软文位置。新增独立合作展示规划与 README 入口，记录拟放置位置、稳定标识、待补素材与状态；当前仅文档预留，尚未实现客户端广告或发布软文。PatchxNote 与 PatchX Freenote 的关系未经确认，分别保留名称。

## 2026-09-25 18:11 · v0.2.1 模型资源与可移植下载流程

用户要求制作版本 Release，将用到的模型作为附件发布，并同步 SKILL；随后明确继续采用 GitHub 分卷方案。准备8组模型：两份 SenseVoice、FSMN/Silero VAD、CAMPPlus、Zipformer KWS、Qwen3-TTS Base 0.6B与1.7B。Qwen大包按1 GiB分卷，模型清单固定来源、大小与分卷/整包/逐文件SHA256，包内保留来源和许可说明。未打包个人录音、音色参考、Python环境或runtime；未切换应用模型。

新增标准库下载器，按选择下载、支持断点续传、验证缓存、合并分卷并在逐文件校验通过后暴露新目录；拒绝覆盖冲突模型。SKILL与README补上下载、注册、实际推理验证的区别，保留两份SenseVoice选择权。版本更新为0.2.1，仍为源码/Skill/模型发布，没有桌面安装包。

验证：8组实际包均通过隔离解压与逐文件校验；5项下载器测试覆盖HTTP续传、缓存复用、分卷合并、坏校验、路径越界及已有模型保护；完整Skill导出/粘贴还原/桥接测试和Skill格式校验通过。本机大附件上传反复连接中断，小附件成功；增加仅手动触发的GitHub Actions构建流程，从固定来源重建并要求与本地既定SHA256完全一致，上传到已有草稿后检查服务端摘要。首次独立重建SenseVoice包摘要一致。此记录时Release仍为草稿，正式发布需等待附件全部校验完成。

## 2026-09-25 18:21 · v0.2.1 Release 附件全部校验完成

GitHub端8组模型构建任务全部通过，14个模型附件（含Qwen分卷）、完整Skill包、模型清单与SHA256SUMS共17个附件已上传。逐项核对GitHub服务端state、大小和SHA256与本地清单完全一致；模型原始ZIP总计约7.61 GB。完整Skill附件逐文件与当前源码一致，现有录音runtime指纹未变。

排错记录：本机大附件上传多次TLS连接中断，改为GitHub Actions拉取固定来源并重建；首次任务发现草稿Release不能依赖按tag查询、ZIP64中央目录与本地文件头版本字段不同。修正草稿查询和ZIP元数据后，Qwen0.6B本地整包/分卷复验一致，GitHub8组任务全部成功。校验未通过时始终保留草稿；最终发布以此后的远端main提交为目标，模型内容与应用选择不变。

## 2026-09-25 18:22 · Release 公开下载验证与同分钟日志排序

v0.2.1 已正式发布，标签指向 aa71d1f，17个附件服务端校验通过。使用发布后的公开URL运行Skill下载器，在全新目录下载Silero包、解压并逐文件校验成功，未注册或改变应用runtime。

收尾时另一任务同步写入FreeNote致谢文档，两条开发记录时间同为18:21。发现原日志脚本只按分钟排序再反转，同时间记录每次生成会交换顺序，导致刚生成后check仍失败。增加按原文的确定性次序，连续生成与check验证幂等；保留双方内容。另一任务的README与展示规划变更由其自身提交保留，没有重置共享工作区。

## 2026-09-25 18:23 · FreeNote 来源致谢与开源产品的协作思考

用户要求在 README 补充 PatchX FreeNote 的介绍与链接，并将我们对开源产品的思考写入相关日志。用户说明已实际接入官方 MCP，在使用中遇到问题，借助 AI 根据执行结果推测策略并优化自己的调用模式，希望用实际改进结果向官方提意见；这不应被误写成已经提取模型权重或确认其私有实现。本次只记录该实践，未把它声明为 SayAgain 已交付的集成功能，也未补造性能数字。

我们的开放协作原则是：尊重上游作者和下游微调、产品改进者的劳动，清楚注明来源；以具体输入、实际结果和可复现改进提出建议，不把不同意见视为恶意；保留用户调整自己工作流程的选择，同时区分实测事实与对内部实现的推断。自己的代码开源、官方接口可调用、模型权重可独立使用或再分发，是不同范围的问题，不能用致谢代替许可，也不能仅凭通用禁令否定所有正常测试和反馈。

README 新增 SenseVoiceSmall 与 PatchX FreeNote 致谢、官方下载及 MCP/Agent 链接。官方 Agent 仓库说明 FreeNote 原名 PatchXNote，补充到展示规划；用户确认官方已说明模型经过 SenseVoiceSmall 微调，保留这一信息的来源层级，未声称独立核实具体权重批次。当前 MCP 文档主要提供记录和已有 AI 结果的访问，不提供触发模型运行；模型许可仍需按具体制品核对。此次文档说明不是正式合作声明，也不对既有模型附件的再分发权限作出新的确认。

用户进一步强调：购买和使用硬件后，通过 MCP 将相关能力接入自己的工作流程，遇到软件体验不足时应继续改进；开放接口应支持这种实践与反馈。用户也指出 Electron/Web 客户端让客户端执行策略更容易观察。本项目将其记录为产品立场：持续改进自有调用流程、以实际结果反馈，同时不把 MCP 读取结果等同于直接控制硬件或运行模型，不把客户端可观察等同于所有服务端策略公开或获得任意复制、分发许可。

参考：[FreeNote 下载](https://freenote.patch-x.cn/download/)、[MCP 接入](https://freenote.patch-x.cn/mcp/setup/)、[Agent 文档](https://github.com/ZsTs119/patchx-freenote-agent)、[SenseVoice 许可说明](https://github.com/QwenAudio/SenseVoice#license)。本条同步生成开发传记与记录图表，验证文档同步和差异格式；未修改应用代码、模型或运行配置。

## 2026-09-25 19:35 · 收紧模型说明，保留原理、来源与体验致谢

用户强调开源项目需要严谨，要求暂缓 README 中模型版本与效果结论，只介绍原理、注明来源模型，并肯定 PatchX FreeNote 的实际表现。README 改为语音区间检测、说话人分段、识别、试听校对的原理说明，明确标点、英文大小写、数字规范化与边界拼接是分别验证的目标，不把它们写成全部完成的能力。

保留 SenseVoiceSmall 与 sherpa-onnx 来源链接，以及 FreeNote 下载、MCP 入口；以开发者实际使用印象致谢 FreeNote 的微调和产品优化，不宣称全面胜出。按用户提供背景，记录我们参与 FreeNote 共创群，希望以实测问题和改进共同完善产品。此前开放协作讨论与模型对照原始记录保留在历史文档中，README 不再展开法律讨论、模型对比或合作广告位规划。既有发布下载说明保持为资源索引，不据此增加模型推荐或授权结论。

本次仅调整说明文字，未切换模型、修改运行配置或发布新的性能结论。同步开发传记及图表，并检查生成文档一致性与差异格式。

## 2026-09-25 19:43 · README 增加 FreeNote 产品图与购买引导

用户提供 PatchX Freenote AI 录音卡宣传图，希望放入 README 帮助推广。原图保存在 docs/images/patchx-freenote-promo.png，在 FreeNote 致谢段落后以720像素宽展示，并链接到官方下载页；增加「产品推荐」说明，引导读者通过官方页面的「购买录音卡」查看购买方式。图片宣传信息归于官方说明，未添加价格、折扣、佣金或正式合作声明。

同步展示规划的素材与发布状态，客户端广告位仍未实现。验证图片与用户原文件 SHA256 一致、README 引用文件存在，文档生成同步与 git diff --check 通过。

## 2026-09-25 19:53 · 记录录音处理流程与 Freenote 策略差异

用户要求把本次流程核对和主要区别写入 README 或开发日志。README 新增「与 Freenote / PatchxNote 的录音处理策略对比」，明确范围是录音文件解析，列出当前输入转换、VAD、CAMPPlus、整批 KMeans、拆条、SenseVoice 自动转写和人工校对的完整链路。

对比覆盖语音检测、批处理与实时增量、说话人分配及后续修正、ASR 切块、跨任务声纹匹配、文字后处理。明确 SayAgain 当前已经拆条后自动识别，不要求先确认说话人；旧日志中的确认前置规则保留为历史，不再用来描述当前行为。

SayAgain 结论依据当前源码；Freenote（现名 PatchxNote）的结论依据此前本机安装包配置、C++ 符号与 Dart 残留信息，未取得完整源码，不宣称所有机制默认启用或完整调用顺序已经确认。相同模型不等于相同结果，本轮未进行同一音频的双端识别质量对照。

本次仅补充文档，不修改录音策略或模型。使用文档同步脚本更新开发传记及统计图，并检查同步一致性。

## 2026-09-25 19:57 · 同步全部相关录音流程文档

用户要求全部更新。同步 README、录音策略、Skill 录音说明、产品说明、桌面交互说明和开发入口；通过脚本同步开发传记与统计图。Skill 文档原先仍要求先确认再转写，并描述已停用的旧 FSMN 分段入口，本次改为当前自动转写流程，补充同会话追加、录音检查点拼接、输入限制、候选标签与人工校对边界。

各入口链接到统一的六项 Freenote / PatchxNote 对照，保留安装包分析的不确定性。旧日期日志保留当时的决定，当前说明明确自动转写已经取代确认前置流程。本次仅更新仓库文档，不变更算法、模型或外部已安装 Skill 副本，不新增识别质量测试结论。执行文档生成一致性、相对文件链接及差异格式检查。

## 2026-09-25 22:50 · 共同模型选型的准确表述与四来源核验

用户要求 README 只表述 Freenote「也选用了同样的」模型，避免由文件一致性延伸出未经验证的开发过程或产品来源关系。README、录音策略与 Skill 说明统一采用「Freenote / PatchxNote 也选用了相同的官方通用模型」，不称其为 Freenote 自研或微调模型。

新增证据：官方 sherpa-onnx 2024-07-17 通用 INT8 下载包通过发布方大小与 SHA-256 校验，其 ONNX 文件与本机 Freenote 内置及 SayAgain Mac 当前使用文件的完整 SHA-256 均为 c71f0ce00bec95b07744e116345e33d8cbbe08cef896382cf907bf4b51a2cd51。此前无法确定对应官方导出包的状态由这次比对补充；不改写旧日志当时的证据状态。安装器当前选择的 2025-09-09 粤语微调版属于另一份权重，明确区分安装脚本与实际运行文件。

四个来源（官方通用、官方粤语、Freenote、闪电说官网指向的量化版）在同一 ONNX Runtime CPU 脚本下，完成公开中英粤样例 × ITN 开关共 24 次推理；通用与 Freenote 的 token 全部一致。统一预处理的通用版结果与独立 sherpa-onnx 的 6 项参考调用一致；禁止测试进程网络访问后，四来源英文样例 × ITN 的 8 项结果仍一致。此结果不等于原应用断网测试或应用整体质量排名；用户专项数字测试仍待语音。真实用户内容、权重与测试音频不加入仓库。

本次只更新文档表述与证据边界，运行文档同步及差异格式检查，不修改应用模型、算法或设置。

## 2026-09-25 23:05 · 客户端展示四个模型来源与校验证据

按用户要求采用中性、可核对的表述，在设置「语音识别模型」、我的音频列表及录音设置中加入来源说明。列出官方通用版、官方粤语微调版、Freenote / PatchxNote 内置样本、闪电说官网推荐量化版，共四个来源、三份不同文件；区分应用包名、公开模型名和 ONNX 格式，不把包名变化解释为自研或微调。

每项可展开查看文件名、大小、完整 SHA-256 和公开来源。Freenote 样本仅表述「也选用了相同的官方通用模型」；闪电说样本明确为按官网指引手动安装的 iic 官方量化文件，未核对其自动下载默认文件。固定来源链接由主进程白名单打开，复制指纹由受限 IPC 写入剪贴板。页面不下载或切换模型，不发送录音。

新增模型来源证据文档并更新 README 与录音策略文档。证据页标注核验日期、公开样例范围及实际应用后处理未覆盖的边界，不代表当前机器安装状态或总体识别质量。

验证：72 项自动测试与 Electron 桌面回归通过；隔离数据目录检查四项展示、来源白名单、非法来源拒绝、完整指纹复制和未知模型拒绝，宽窄界面已检查；我的音频入口显示四项且无渲染异常。最初浏览器剪贴板调用未稳定完成，改为原生受限操作后通过验证。未使用用户录音，未修改用户模型或数据。 推送前合并远端同期更新，保留发布资源、Windows 修复与 FreeNote 致谢；将致谢中未经本次样本支持的微调表述改为产品优化。合并后 74 项自动测试通过。

## 2026-09-25 23:20 · 撤下 FreeNote 推荐并收紧来源表述

用户重新评估此前的致谢与推荐措辞，要求正视应用模型包名称与上游文件来源的区别。README 改为上游模型致谢与第三方样本核验，撤下产品推荐、购买引导及宣传图展示；原展示规划同步标为停止推进，历史日志保留当时状态。

依据为已核验 PatchxNote 1.0.2（21）内置模型与官方通用 INT8 文件大小和完整 SHA-256 一致，明确当前样本不支持另行微调权重的表述。标点、大小写与数字呈现不能直接归因于厂商微调；结论不延伸至其他版本、云端模型、全部产品工作或动机。私人聊天及截图未写入仓库，未作为公开引文。

验证：文档同步、推荐入口撤下及差异格式检查；仅修改说明，不改模型、运行配置或用户数据。

## 2026-09-25 23:22 · 恢复产品推荐并明确上游能力归属

用户澄清：保留 FreeNote 商业化录音产品与购买推荐，但不使用致谢措辞，不将标点等上游能力归功于厂商微调。README 恢复现有宣传图和购买入口，在相邻文字说明已核验样本就是相同的官方通用模型文件；官方模型直接推理已经体现标点、大小写及部分数字规范化，产品包名不能作为独立微调证据。

同步产品展示说明、客户端来源卡片及证据文档。推荐对象为商业化硬件与应用集成，文件结论限于已核验版本；不推断其他版本、云端模型或厂商动机。前一次撤下推荐的记录保留，此条记载用户澄清后的当前决定。未新增客户端广告位，未上传私人截图或聊天。

验证：文档生成同步、共享数据脚本语法、宣传图片引用及差异格式检查通过。仅变更文案与展示说明，不修改模型或用户数据。

## 2026-09-25 23:24 · 删除微调归功判断措辞

按用户要求，删除 README 中「不能归功于 FreeNote 的独立微调」及客户端来源说明、证据页、产品说明中的同义归因判断。保留上游能力说明、商业化产品推荐和已核验文件一致性的事实。历史日志保留原调整经过。

验证：相关文案、共享脚本语法、文档同步及差异格式检查通过。

## 2026-09-25 23:33 · README 汇总已核验模型事实与测试范围

按用户要求通读 README，补齐四来源文件核验、通用版与粤语版标点差异、大小写和数字样例结果、24 次统一推理、6 组 sherpa 交叉核对及 8 次进程禁网测试。明确手动安装的闪电说样本与默认下载文件的区别、文件提交日期与训练日期的区别、SayAgain 实际登记文件与安装器选择的区别。

新增仅含公开样例的四来源原样输出文档，链接完整指纹与来源证据。日期、金额、小数、百分比、长数字专项语音、长录音衔接、原应用完整链路与整机断网测试保持待验证；未以有限样例排名整体质量。修正旧产品名称关系，更新进度日期、已完成的 74 项测试记录和平台验证范围。保留用户确认的产品推荐及中性措辞，不重新加入已删除的微调归功判断。

验证：逐项对照既有本地实验报告与来源证据，检查公开样例输出、文档链接、日志生成及差异格式。此次未重新运行模型或上传个人音频、路径、聊天和截图。

## 2026-09-25 23:46 · README 按四项产品优先级重排

用户明确产品主次：表达优化最优先，其次用自己的声音，再次音频文件与录音，最后声纹角色库。README 首屏定位、功能目录与正文依此重排；表达实例及截图归入第一功能，音色参考与合成说明归入第二功能，人物资料与关联从音频表格独立归入第四功能。

模型核验、上游来源及第三方产品推荐移到后面的技术说明，保留全部核验表、证据入口和购买推荐。录音细节与导入排查折叠，避免技术内容压过核心功能。明确声纹角色库目前实现人物管理与人工关联，未实现跨会话自动身份匹配；保留本地 TTS 与逐轮触发待验证的状态。

验证：检查重排后的章节顺序、本地链接、图片归属、折叠标签配对和证据内容保留；文档同步与差异格式检查通过。仅调整 README 信息结构与说明，未修改产品代码。

## 2026-09-25 23:53 · 公开截图实现与基于对标的排查复盘

用户要求公开截图能力的解决过程，强调展示解决问题的思路与有效对标，而非炫技。回查当日开发会话，确认首次反馈为18:45:15，原型核验完成为19:03:40，产品集成从19:08:14到19:30:15；对应18分25秒、22分1秒与全程45分钟，均注明为会话经过时间而非纯编码工时。

新增复盘记录系统截图、Pillow复用路径、权限与窗口枚举、ScreenCaptureKit、AVCaptureScreenInput、SDK直接调用失败及动态解析原型等尝试。明确参考贝锐向日葵：先有用户的成功对照，再以组件线索、运行日志与独立程序验证。保留早期归因过快、误把包装库当独立路线的修正，不推广为行业排名或永久兼容。晚间闪电说无法捕获微信仅记为用户实测反馈，未冒充此次开发复现。

将此前已接入但未提交的截图组件、快捷键设置与产品线优先级协调源码纳入本次公开提交。新增独立 screen-capture-troubleshooting Skill，供复用排查和版本化记录方法；README 保持辅助能力位置。公开文档标注系统与整理时应用版本、API弃用状态、复验触发条件及失败行为；不提交原生构建产物、私人画面、完整日志或账号信息。此次仅在 SayAgain 仓库发布，没有代替 Director 仓库的发布或生成新安装包。

验证：6项截图协调测试、74项应用测试、Electron桌面回归、原生构建命令与Skill格式校验通过；链接、耗时算式、脚本语法和差异格式检查通过。此次未重跑会截取真实桌面的截图烟测；微信捕获和双产品截图链路引用当日原始实测，明确区别于本轮回归。

## 2026-09-26 08:49 · 补齐整理、恢复、录音优化与跟读复习

按用户“逐个补完”的要求，补齐草稿离开提醒、备份校验与恢复前安全备份、彻底删除后的 Skill 引用修复、人物归档与合并、录音选句优化和来源跳转、跟读录音与自评复习、文本字幕音频导出及批量归档恢复。沿用普通列表归档、归档列表内彻底删除的约定。

文本优化服务使用独立配置与密钥，需用户配置接口后明确发送；没有复用语音密钥。跟读保存实际录音与自评历史，不宣称自动发音评分。人物恢复不逆转合并转移。备份恢复保留本机密钥并停止恢复数据中的遗留进行中任务，避免自动发起收费调用。

验证：85 项自动测试、原桌面回归、单条编辑归档验证、新增完整工作流验证通过。新增链路使用临时数据、本机模拟文本接口和浏览器测试麦克风，覆盖字幕落盘与备份恢复；没有将真实远端模型调用或真人跟读质量记为已验证。原桌面测试适配新增的未保存提醒。

## 2026-09-26 08:57 · 按反馈移除跟读与复习

用户认为当前跟读与复习不合适，要求先删除。移除侧栏页面、表达上的跟读按钮、录音自评界面和新增练习接口；保留已有练习数据与音频，原有语音合成与播放不受影响。更新工作流回归，检查页面入口和写入接口已移除。

## 2026-09-27 00:02 · iOS 独立目录、阶段归档与双构建路线

按用户要求将 iOS 阶段成果整理入 SayAgain 项目。保留同一仓库下的 `ios/`，新增 `ios/docs/milestones.md` 成果索引和 `ios/docs/distribution.md` 发行说明，根 README 添加入口。此处时间是整理时间，以下首轮结果来自 9 月 26 日真机试验。

保留用户对初版回顾 UI 简陋、模型选择/音量/字号不完整、音频缺少波形的反馈。后续按 Electron 实际截图调整原句、建议、解释和语音布局，接入真实 PCM 波形、播放定位和全局设置；具体测试证据见 iOS validation，不能将实现和自动测试写成用户已验收。

iPad mini 3 / iOS 12.5.8 已完成原生 SenseVoice INT8 前 10 秒识别：加载 22.55 秒、推理 15.66 秒、总耗时 40.50 秒、采样进程内存峰值 301.3 MiB。使用桌面现有模型权重和原生 sherpa/ORT；不是在 iPad 嵌入 Electron/Python，也未完成本机克隆 TTS 或说话人识别。原录音和文字未改写，未做飞行模式验收。

新增 `Config/Legacy-iOS12.xcconfig` 与 `Config/AppStore.xcconfig`，共享 Swift 源码，分离部署目标与应用标识。旧版继续用于已注册旧 iPad 开发安装；现代版最低 iOS 15，Xcode 26.6 / SDK 26.5 无签名 Release 编译通过。尚未签名 Archive、上传 App Store、审核或现代设备功能验收。现代编译仍有旧 UIKit API 弃用和 Swift Sendable 提示，不宣称零警告或完整发行就绪。

核对 Apple 官方：2026-04-28 起提交要求 Xcode 26+ / SDK 26+；Xcode 26.6 上架部署目标从 iOS 15 起。2027 年 4 月的 SDK 27 要求属于将来规则。依据及命令留在发行说明，避免将新 SDK 要求误解为只能支持最新运行系统，或把旧 iOS 12 开发包当成当前可上架包。

## 2026-09-27 00:16 · mini 3 完成四分钟录音的本机转写

将前 10 秒试验扩展为整条录音：模型只加载一次，约 7–10 秒有界缓冲，优先在低能量窗口切段；每段完成原子保存独立 ASR 草稿，重新打开可读回。采用草稿需明确确认，只填入编辑框供校对后保存；不自动改写原文。停止在当前步骤结束后生效，并补前台识别时临时防止自动锁屏、后台请求停止。不是学习型 VAD、逐词时间对齐或说话人识别。

真实 iPad mini 3 / iOS 12.5.8 完整处理已有 241.42 秒录音，共 29 段。模型加载 25.03 秒、推理累计 640.31 秒、App 内总耗时 811.08 秒，采样进程内存峰值 311.6 MiB。真机完整转写、原文保持不变、终止重启后草稿恢复的测试通过（1 项、0 失败）；原录音保留。

这证明四分钟录音可以在旧设备本机处理，但约 13.5 分钟的总耗时尚未达到实时。测试为带 UI 自动化的 Debug 场景，没有宣称无干扰性能、逐字准确率或飞行模式已验收。草稿旁路 JSON 暂不包含在资料 JSON 导出中，完整 Documents 文件共享备份可保留；不提供断点续算。证据、限制和 bundle 路径记录在 `ios/docs/validation.md`。

另一次真机测试通过：模型加载期间取消新任务，之前完整草稿和原文字保留，重启读回一致（1 项、0 失败）。该测试不涵盖推理中途、后台或系统强杀；当前只有一份 ASR 草稿，不保留多轮识别历史。最新代码已安装到设备。

同步脚本增加大模型与原生库 SHA-256 比对，只有与远端内容一致的文件才跳过传输，避免每次小改动重复发送大模型。iOS 阶段索引、发行路线、根开发传记和图表同步更新。

## 2026-09-27 00:27 · 将 iOS 上架版与老设备版拆成可见入口目录

用户指出仅在 Config 中区分配置，访客无法直观看出哪个版本准备上架、哪个版本给老设备自用。将现代配置移至 `ios/app-store/`，将 Legacy 配置及远端构建脚本移至 `ios/legacy/`；两个目录各有独立 README、用途、系统要求和构建命令，现代目录另提供无签名编译入口。根 README 与 iOS 首页明确列出两条路线，共享 UIKit 源码与 Xcode 工程保持一份。

更新同步脚本携带新目录并使用 Legacy 新路径，部署目标与 Bundle ID 保持原值；开发成果与发行文档的现行路径同步更新。目录名表示发行目的，不声称已经通过 App Store 审核。

## 2026-09-27 00:46 · iPhone 12 Pro 同音频本机测速与浅色界面修复

按用户要求在本机连接的 iPhone 12 Pro / iOS 26.6.1 上编译并开发签名安装现代版。只从 iPad 提取对应原始 WAV，传至 iPhone 后回读验证 SHA-256 相同，241.4175 秒、24kHz 单声道；模型权重、原生运行时与 CPU 单线程设置一致。

Release 两轮总耗时 69.22 / 69.15 秒，后者模型加载 1.44 秒、推理 66.93 秒、采样进程内存峰值 418.02 MiB。真正关闭 Swift 优化的 Debug 对照总耗时 73.88 秒、推理 67.09 秒。相对旧 iPad 的 811.08 秒，当前样例总耗时约缩短至 1/11.7；两个 Debug 对比约 1/11.0。这是跨设备/系统的单样例实测，不是固定硬件加速比。

保留调试过程：首次 Debug 命名构建被 xcconfig 覆盖为实际 -O，68.95 秒不能作为未优化对照；检查 swiftc 参数后，新增明确 -Onone 的 Benchmark-Debug.xcconfig 重测。后两轮实际记录低电量模式开启、首尾系统热状态 serious，未改变用户电源设置，也没有宣称满性能凉机结果。

真机截图发现 iPhone 深色模式下表单白底白字，以应用浅色外观修复；不改系统主题。新增可选的电源/热状态字段，旧 Mac 的 iOS 12 目标编译通过。四轮完整转写、原文不变及重启恢复均通过，最终留在 iPhone 的是 Release 版及同一录音，保存 Release/Debug 两份报告。阶段文档、去除全文的测量 JSON 和视觉对比入库，音频与用户转写全文不提交 Git。详见 `ios/docs/iphone12pro-benchmark.md`。
